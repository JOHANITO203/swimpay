# Task 537 — Google OAuth BFF provider seam

Status: completed

Scope:
- Add a Google OAuth/BFF provider seam.
- Production must fail closed when Google provider configuration is missing.
- Development may use a guarded dev bootstrap route.

Constraints:
- No Google tokens or client secret in frontend/browser-readable state.


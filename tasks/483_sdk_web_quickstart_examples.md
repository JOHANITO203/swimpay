# Task 483 - SDK Web quickstart and examples

Sprint: 9B - SDK Web Production Readiness

Goal:
Add production-ready SDK Web quickstart docs and a minimal Node example.

Acceptance:
- Add `docs/SDK_WEB_QUICKSTART.md`.
- Add `examples/web-node-basic/` if repo convention allows.
- Include server-side order creation, redirect to `checkout_url`, webhook raw-body verification and public event handling.
- Include `.env.example`.
- Include security notes and product truth warnings.
- Add docs guardrail tests.

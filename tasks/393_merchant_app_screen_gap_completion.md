# Task 393 — Merchant app screen gap completion

Status: completed

Scope:
- Add missing frontend merchant screens without API or business logic changes.
- Cover dashboard, receiving methods, banks, review queue/detail, orders/detail, connected site, receiver phone, tests and settings.

Acceptance:
- Routes render for `/merchant/banks`, `/merchant/orders`, `/merchant/orders/:orderId`, `/merchant/receiver-phone`, `/merchant/tests`, `/merchant/settings`, `/merchant/connected-site`.
- Screens use simple merchant wording and masked-only values.

# Task 690 - Android Security / App Lock

Status: pending

Objective: implement the `Securite` screen as device-level UI protection.

Requirements:
- enable/disable app lock;
- Android biometric/device credential prompt when available;
- timeout choices: immediately, 1 min, 5 min, 15 min;
- local persistence;
- require unlock when the app resumes after timeout;
- no custom PIN storage;
- do not block background receiver upload.

Output:
- `.swimpay-agent/ANDROID_SECURITY_APP_LOCK_REPORT.md`

# Task 252 - Operator Secret and Token Handling Runbook

Document local signed-token handling and production gaps.

Requirements:

- local token helper remains development-only;
- generated tokens must never be committed;
- production operator lifecycle remains a future hardening item;
- document future controls: issuance, rotation, revocation, secret storage, break-glass and audit.


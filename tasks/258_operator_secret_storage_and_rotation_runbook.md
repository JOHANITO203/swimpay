# Task 258 - Operator Secret Storage and Rotation Runbook

Document secret storage and rotation expectations.

Requirements:

- secrets must stay outside git;
- generated local tokens must not be committed;
- production secret storage must be external;
- rotation must retire old credentials and verify old tokens fail.


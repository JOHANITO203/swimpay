# 210 Operator Evidence Rehearsal CLI

Status: completed

## Goal

Provide a lightweight operator CLI for evidence lifecycle UI/API rehearsal.

## Completed

- Added `scripts/evidence-lifecycle-rehearsal.mjs`.
- Added `npm run rehearsal:evidence`.
- The script supports a non-destructive `--plan` mode and a local API validation mode.


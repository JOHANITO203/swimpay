# Task 550 — Production SDK API key validation

Status: completed

Scope:
- Validate `/v1/orders` production API key identity.
- Reject invalid/revoked/test keys.
- Reject `auto_confirm` / `autoConfirm`.
- Preserve payment confirmation semantics.


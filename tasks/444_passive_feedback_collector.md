# Task 444 - Passive Feedback Collector

Implement passive feedback models and a safe backend ingestion endpoint. Feedback must be stored/returned as supervised monitoring data only and must never mutate runtime classifier rules.

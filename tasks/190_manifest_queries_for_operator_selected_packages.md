# 190 Manifest Queries For Operator Selected Packages

Status: completed

Add minimal package visibility for the Sprint 4Q operator-selected package.

Implemented:

- debug manifest adds an exact `<queries>` entry for `ru.sberbankmobile`;
- main manifest remains free of real package queries;
- no broad package visibility was added;
- no auto-confirmation or production trust behavior was added.

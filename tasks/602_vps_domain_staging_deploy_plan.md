# Task 602 - VPS / domain staging deploy plan

Status: completed

Goal: prepare a concrete deployment plan for `staging.swimpay.pro` with HTTPS, Docker Compose services, logs and healthchecks.

Rules:
- Do not deploy public production.
- DNS expected: `A staging.swimpay.pro -> <VPS IPv4>`.

Deliverable:
- `.swimpay-agent/VPS_DOMAIN_STAGING_DEPLOY_PLAN.md`

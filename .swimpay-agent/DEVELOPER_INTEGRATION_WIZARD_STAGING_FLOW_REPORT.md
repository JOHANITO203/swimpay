# Developer Integration Wizard Staging Flow Report

generated_at: 2026-05-08T22:00:00+03:00

## Result

The Developer Integration Wizard is complete enough to prepare the SDK/webhook rehearsal for a staging merchant.

Verified:

1. Private API key lifecycle is show-once on create/rotate, masked on normal reads.
2. Webhook secret lifecycle is show-once on create/rotate, masked on normal reads.
3. Webhook URL is saved through the merchant-scoped backend integration API.
4. Test webhook is backend-owned, test-only and cannot trigger fulfillment.
5. Web snippets use `@swimpay/node` server-side order creation and webhook verification.
6. Android snippets open `checkout_url`, parse the return intent and never include secrets or webhook handling.
7. The wizard now renders an external-app export block for staging values.

## Export Block

The wizard now shows:

```text
SWIMPAY_STAGING_API_BASE_URL=<api base url>
SWIMPAY_STAGING_SECRET_KEY=<show-once raw key immediately after create/rotate, otherwise masked key>
SWIMPAY_STAGING_WEBHOOK_SECRET=<show-once raw secret immediately after create/rotate, otherwise masked secret>
SWIMPAY_WEBHOOK_URL=<configured webhook url>
EXTERNAL_APP_BASE_URL=https://<merchant-staging-endpoint>
SWIMPAY_PUBLIC_WEBHOOK_EVENTS=payment.confirmed,payment.rejected,payment.expired
```

Raw secrets still appear only in immediate action responses. Normal reads remain masked.

## Safety

- No Android/browser snippet contains `SWIMPAY_SECRET_KEY` or `SWIMPAY_WEBHOOK_SECRET`.
- Public events remain only `payment.confirmed`, `payment.rejected`, `payment.expired`.
- Test webhook keeps `test_only=true`, `triggers_fulfillment=false`, `official_bank_confirmation=false`.
- No real bank notification was processed.
- No auto-confirmation or public webhook semantics were changed.

## Fresh Root Validation

Passed on 2026-05-08T22:06:00+03:00:

- `npm run android:doctor`
- `npm run typecheck`
- `npm run lint`
- `npm test` - 74 files, 530 tests passed
- `npm run build`
- `docker compose --env-file .env.example -f infra/docker-compose.yml config`

## Staging Check

Checked on 2026-05-08T22:21:00+03:00:

- `https://staging.swimpay.pro/api-health` returned production mode with database, NATS and Valkey `ok`.
- `https://staging.swimpay.pro/merchant/developer-integration` returned HTTP 200.
- The page contains the new `Variables staging pour app externe` export block.
- The page still renders a pending/unavailable merchant connection state from this unauthenticated shell session.

## Current Blocker

Credential generation was not executed from this shell because no staging merchant BFF session/CSRF material, approved server-side merchant integration token, staging DB URL or external merchant app env target is available here.

Required operator-side values/actions:

- authenticated staging merchant dashboard session, or
- explicit staging seed output stored securely outside chat:
  - `swimpay_bff_session`
  - `x-csrf-token`
  - staging merchant context
- external merchant app environment target for:
  - `SWIMPAY_STAGING_SECRET_KEY`
  - `SWIMPAY_STAGING_WEBHOOK_SECRET`
  - `EXTERNAL_APP_BASE_URL`

Do not paste show-once API keys or webhook secrets into chat.

## Prior Validation

Passed:

- `npm test -- apps/web/src/developer-wizard.test.ts`
- `npm test -- apps/api/src/developer-integration.test.ts`
- `npm run typecheck -- --pretty false`

Pending before final closeout:

- root lint
- full test suite
- build
- compose config

## Android Implementation Update

Checked on 2026-05-08T22:39:00+03:00.

The Android merchant app now uses the same backend-owned Developer Integration Wizard contracts as the web surface.

Implemented:

1. Android mobile `spm_...` merchant sessions can call `/v1/merchant/integration*` through scoped mobile permissions.
2. Web BFF sessions remain CSRF-protected for write actions; Android bearer sessions do not require web CSRF.
3. The APK has a real `Integration developpeur` screen under Menu.
4. The screen can:
   - load merchant integration state;
   - create an API key;
   - rotate an API key;
   - rotate a webhook secret;
   - save webhook URL;
   - send the backend-owned test webhook;
   - show staging export values for an external app.
5. Show-once raw API key / webhook secret values are held only in the immediate Android UI action state.
6. Normal reads remain masked.
7. Android snippets and browser snippets still contain no secrets and Android does not handle webhooks or fulfillment.

Safety preserved:

- SDKs consume values generated by SwimPay backend; SDKs do not generate API keys or webhook secrets.
- Android does not confirm payments.
- Android does not send developer webhooks directly.
- No `payment.confirmed` semantics changed.
- No real bank notification was processed.

Fresh validation for this Android implementation:

- `npm run android:doctor`
- `npm run typecheck`
- `npm run lint`
- `npm test` - 74 files, 531 tests passed
- `npm run build`
- `docker compose --env-file .env.example -f infra/docker-compose.yml config`
- `apps/android-receiver/android/gradlew.bat -p apps/android-receiver/android :app:testDebugUnitTest --no-daemon --stacktrace --max-workers=1`
- `apps/android-receiver/android/gradlew.bat -p apps/android-receiver/android :app:assembleDebug --no-daemon --stacktrace --max-workers=1`

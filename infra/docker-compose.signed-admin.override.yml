services:
  swimpay-api:
    environment:
      ADMIN_AUTH_MODE: signed_token
      ADMIN_TOKEN_HMAC_SECRET: ${ADMIN_TOKEN_HMAC_SECRET:?set ADMIN_TOKEN_HMAC_SECRET for local signed-token rehearsal}
      DEV_ADMIN_TOKEN: ""
      DEV_ADMIN_OPERATOR_ID: ""
      DEV_ADMIN_ROLE: ""

  swimpay-web:
    environment:
      ADMIN_AUTH_MODE: signed_token
      ADMIN_TOKEN_HMAC_SECRET: ${ADMIN_TOKEN_HMAC_SECRET:?set ADMIN_TOKEN_HMAC_SECRET for local signed-token rehearsal}
      DEV_ADMIN_TOKEN: ""
      DEV_ADMIN_OPERATOR_ID: ""
      DEV_ADMIN_ROLE: ""
